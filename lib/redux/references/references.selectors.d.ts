import { IStoreState } from "../types";
/** returns the id of the viewport
 */
export declare function getViewportId(state: IStoreState): string;
/** returns the id of the editor
 */
export declare function getEditorId(state: IStoreState): string;
