import { IStoreState } from "../types";
import { ISelection } from "./selection.types";
/** returns an array with the ids of selected elements
 */
export declare function getSelection(state: IStoreState): ISelection;
